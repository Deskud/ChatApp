<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ListViewItem" x:Key="ChatItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="20" Height="20">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding ImageSource}" RenderOptions.BitmapScalingMode="Fant"></ImageBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="{Binding Username}" Foreground="{Binding UsernameColor}" VerticalAlignment="Center"></Label>
                                <Label Content="{Binding Time}" Foreground="Gray" FontSize="10" VerticalAlignment="Center"></Label>
                            </StackPanel>
                        </StackPanel>
                        <Label Content="{Binding Message}" Foreground="White" FontSize="20" VerticalAlignment="Center"></Label>
                    </StackPanel>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>